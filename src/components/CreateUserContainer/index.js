import styled from "styled-components";
import DefaultContainer from "../Container";

const CreateUserContainer = styled(DefaultContainer)`
  height: 560px;
  flex: 1;
  justify-self-center;
  align-self-center;
`;

export { CreateUserContainer };
